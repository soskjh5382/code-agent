[project]
name = "code-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kjh", email = "jhkim91@eseict.com" }
]
requires-python = ">=3.14"
dependencies = [
    "anthropic>=1.3.0",
    "fastapi>=0.141.1",
    "google-genai>=2.21.0",
    "openai>=3.7.0",
    "python-dotenv>=1.2.3",
    "uvicorn>=0.52.4",
]

[project.scripts]
code-agent = "code_agent:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
